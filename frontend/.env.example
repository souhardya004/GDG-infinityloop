# Backend API base URL (include /api/v1). Required for production on Vercel.
# Local dev: leave unset — Vite proxies /api to http://127.0.0.1:8000
VITE_API_BASE_URL=https://your-api.onrender.com/api/v1
